# Build artifacts
target/
node_modules/
dist/
.next/

# Brain data (personal — never commit)
# Belt-and-suspenders: data lives at ~/.neurovault/ outside the repo,
# but guard against any accidental in-repo copy.
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-journal
*.bin
*.parquet
*.jsonl
data/
vault/
export/
backups/
compaction/
cold/
cold_storage/
finetune/
logs/
cache/
.neurovault/
settings.json

# Brain export artifacts (personal content)
brain-knowledge.json
brain-briefing.md
brain-index.json
training-data.jsonl
active-context.md
active-suggestions.md

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
*.log

# Tauri dev
src-tauri/target/
src-tauri/target
src-tauri/target/
src-tauri/gen/

# Local cargo config (may contain machine-specific paths)
src-tauri/.cargo/
.cargo/config.toml

# Claude Code local settings (may contain absolute user paths)
.claude/
